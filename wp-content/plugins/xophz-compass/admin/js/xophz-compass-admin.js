(function( $ ) {
	'use strict';

	/**
	 * All of the code for your admin-facing JavaScript source
	 * should reside in this file.
	 *
	 * Note: It has been assumed you will write jQuery code here, so the
	 * $ function reference has been prepared for usage within the scope
	 * of this function.
	 *
	 * This enables you to define handlers, for when the DOM is ready:
	 *
	 * $(function() {
	 *
	 * });
	 *
	 * When the window is loaded:
	 *
	 * $( window ).load(function() {
	 *
	 * });
	 *
	 * ...and/or other possibilities.
	 *
	 * Ideally, it is not considered best practise to attach more than a
	 * single DOM-ready or window-load handler for a particular page.
	 * Although scripts in the WordPress core, Plugins and Themes may be
	 * practising this, we should strive to set a better example in our own work.
	 */

	 $( window ).load(function() {

        // var theme = 'flat';
        // $.globalMessenger({ theme: theme });
        // Messenger.options = { theme: theme  };

        // var loc = ['bottom', 'center'];
        // var update = function(){
        //     var classes = 'messenger-fixed';
        //     for (var i=0; i < loc.length; i++)
        //         classes += ' messenger-on-' + loc[i];
        //
        //     $.globalMessenger({ extraClasses: classes, theme: theme  });
        //     Messenger.options = { extraClasses: classes, theme: theme };
        // };
        // update();
        // var message ="It's Dangerous to Go Alone! Here, Take my Compass and go exploring. If you get lost, visitssss ";

        // Messenger().post({
        //     message: "<i>Xoph: </i><img src='/wp-content/plugins/xophz-compass/assets/xoph.png' width=177 class='pull-left'/>"+message+"<a target='_blank' href='https://mycompassconsulting.com'>mycompassconsulting.com/help</a>",
        //     hideAfter: 30,
        //     type: 'success',
        //     showCloseButton: true
        // });

    });
})( jQuery );
